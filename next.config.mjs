// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   i18n: {
//     locales: ['en', 'es', 'fr', 'de'],
//     defaultLocale: 'en',
//     localeDetection: false, // Add this line
//   },
//   reactStrictMode: true,
// };

// export default nextConfig;


/** @type {import('next').NextConfig} */
const nextConfig = {
  // If you are using [locale] folders, 
  // you usually don't need the i18n object here anymore.
  reactStrictMode: true,
};

export default nextConfig;